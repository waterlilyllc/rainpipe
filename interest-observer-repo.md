# Interest Observer - 新規プロジェクト

## リポジトリ名
`interest-observer`

## プロジェクト概要
ブックマークから自動的に関心ワードを抽出し、Google検索で日次観測して新しいトピックを収集するパーソナルインテリジェンスツール

## リポジトリ構造
```
interest-observer/
├── README.md
├── .env.example
├── .gitignore
├── Gemfile
├── Rakefile
├── config/
│   ├── database.yml
│   └── schedule.rb          # whenever設定
├── lib/
│   ├── keyword_extractor.rb    # GPTでキーワード抽出
│   ├── daily_observer.rb       # Google検索で日次観測
│   ├── report_generator.rb     # 週次レポート生成
│   └── models/
│       ├── interest_word.rb
│       └── observed_topic.rb
├── data/
│   ├── interests/           # 関心ワードデータ
│   ├── topics/             # 観測したトピック
│   └── reports/            # 生成されたレポート
├── scripts/
│   ├── setup.sh
│   ├── daily_run.rb
│   └── weekly_report.rb
├── web/                    # Sinatra Web UI
│   ├── app.rb
│   ├── views/
│   └── public/
└── test/
    └── test_helper.rb
```

## 新規リポジトリのREADME.md

```markdown
# Interest Observer 🔍

あなたのブックマークから関心事を学習し、毎日新しい情報を見つけてくるパーソナルAIアシスタント

## 特徴

- 🤖 GPTがブックマークから関心ワードを自動抽出
- 🔎 Google検索で1日1回、新しいトピックだけを収集
- 📊 興味の移り変わりを自然に追跡（1ヶ月で自動フェードアウト）
- 📧 週次で重要なトピックをメール配信
- 💰 低コスト運用（月45円程度）

## セットアップ

### 1. 必要なAPIキー
- OpenAI API Key
- Google Custom Search API Key
- Custom Search Engine ID

### 2. インストール
```bash
git clone https://github.com/yourusername/interest-observer.git
cd interest-observer
bundle install
cp .env.example .env
# .envにAPIキーを設定
```

### 3. 初回実行
```bash
# Raindropからブックマークをインポート（オプション）
ruby scripts/import_bookmarks.rb

# 手動でキーワード抽出実行
ruby scripts/extract_keywords.rb

# 日次観測を手動実行
ruby scripts/daily_run.rb
```

### 4. 自動実行設定
```bash
# crontabに追加
whenever --update-crontab
```

## 使い方

### Web UI
```bash
ruby web/app.rb
# http://localhost:4567 にアクセス
```

### コマンドライン
```bash
# 関心ワード一覧
rake interests:list

# 今週のトピック確認
rake topics:this_week

# レポート生成
rake report:generate
```

## 設定

### 関心ワードの調整
- Web UIから追加/削除
- 自動的に1ヶ月で非アクティブ化

### 観測頻度
- デフォルト: 1日1回（朝9時）
- 最大10キーワード/日

## コスト
- Google API: 無料（100クエリ/日以内）
- OpenAI API: 約$0.01/日
- 月額: 約45円

## ライセンス
MIT
```

## GitHub Issues（新規プロジェクト用）

### Milestone 1: MVP (v0.1.0)
- [ ] #1 プロジェクト初期セットアップ
- [ ] #2 GPTキーワード抽出機能
- [ ] #3 Google検索統合
- [ ] #4 基本的なCLIコマンド

### Milestone 2: Automation (v0.2.0)
- [ ] #5 cron自動実行
- [ ] #6 週次レポート生成
- [ ] #7 メール配信機能
- [ ] #8 既読URL管理

### Milestone 3: Web UI (v0.3.0)
- [ ] #9 Sinatra Web UI
- [ ] #10 関心ワード管理画面
- [ ] #11 トピック閲覧機能
- [ ] #12 レポート履歴

### Milestone 4: Enhanced (v1.0.0)
- [ ] #13 Slack/Discord通知
- [ ] #14 複数ユーザー対応
- [ ] #15 エクスポート機能
- [ ] #16 高度な分析機能

## 技術スタック
- **言語**: Ruby 3.0+
- **フレームワーク**: Sinatra (Web UI)
- **データ**: JSON/SQLite
- **スケジューラー**: whenever
- **API**: OpenAI, Google Custom Search
- **配信**: SendGrid/AWS SES

## 差別化ポイント
1. **Rainpipeとは別系統** - ブックマーク管理ではなく、インテリジェンス収集に特化
2. **シンプル** - 統計処理はGPT任せ、複雑なDBなし
3. **実用的** - 1日1回、本当に新しい情報だけ
4. **低コスト** - 月45円で運用可能